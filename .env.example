# Variables d'environnement Pi* — Railway
# Copier ce fichier en .env pour le dev local (git ignore le .env)

# Discord webhook pour signaux live (obligatoire)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Binance base URL (optionnel — defaut: https://api.binance.com)
# BINANCE_BASE_URL=https://api.binance.com
